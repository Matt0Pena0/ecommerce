# import pandas as pd

# class DataFrameTransformer:
#     """Transforma la lista de dicts en un DataFrame de pandas."""
#     @staticmethod
#     def to_dataframe(rows: list[dict]) -> pd.DataFrame:
#         return pd.DataFrame(rows)
